<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<title><%= title %></title>
		<script src="https://cdn.tailwindcss.com"></script>
	</head>
	<body>
		<div class="flex justify-center items-center h-screen">
			<div class="text-center">
				<h1 class="text-4xl font-bold">Vyber kurz</h1>
				<!-- create selector that can hold multiple values selected -->
				<form action="/step-2" method="POST">
					<select
						name="courses"
						id="courses"
						class="mt-4 block w-full py-2 px-3 border border-gray-300 bg-white rounded-md shadow-sm focus:outline-none focus:ring-indigo-500 focus:border-indigo-500 sm:text-sm"
						multiple
						required
					>
						<% for (const course of courses) { %>
						<option value="<%= course.id %>"><%= course.name %></option>
						<% } %>
					</select>

					<button
						type="submit"
						class="mt-4 inline-flex justify-center py-2 px-4 border border-transparent shadow-sm text-sm font-medium rounded-md text-white bg-indigo-600 hover:bg-indigo-700"
					>
						Pokračovat
					</button>
				</form>
			</div>
		</div>
	</body>
</html>
